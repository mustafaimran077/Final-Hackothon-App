// import app from "../src/server.js";
// import connectDB from "../src/lib/lib.js";

// // Establish the database connection when the function starts up.
// // 'connectDB' is smart enough to use the cached connection after the first run.
// connectDB();

// // Export the Express app directly. Vercel automatically wraps it.
// export default app;